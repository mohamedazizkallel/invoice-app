# Notification Clearing System Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add read/dismiss functionality to the NGSign notification bell, an error detail modal, and a dedicated notifications history page.

**Architecture:** New `NotificationState` model tracks per-user read/dismissed state for each `GovInvoice`. The existing `ngsign_pending_api` endpoint is modified to respect this state. Two new AJAX endpoints handle marking-as-read and dismissing. A new notifications page shows full history with filtering and pagination.

**Tech Stack:** Django, PostgreSQL, Bootstrap 5, vanilla JS (fetch API)

**Spec:** `docs/superpowers/specs/2026-03-24-notification-clearing-design.md`

---

## File Structure

| File | Action | Responsibility |
|---|---|---|
| `invoice/sales/models.py` | Modify | Add `NotificationState` model at end of file |
| `invoice/sales/migrations/XXXX_notificationstate.py` | Create | Auto-generated by `makemigrations` |
| `invoice/sales/views.py` | Modify | Modify `ngsign_pending_api`, add 3 new views |
| `invoice/sales/urls.py` | Modify | Add 3 new URL patterns + import new views |
| `invoice/templates/partials/base.html` | Modify | Update dropdown HTML, add CSS, add error modal, update JS |
| `invoice/templates/sales/notifications.html` | Create | Notifications history page template |

---

### Task 1: Add NotificationState model

**Files:**
- Modify: `invoice/sales/models.py` (append after last model class)

- [ ] **Step 1: Add the model**

Add `from django.conf import settings` to the imports at the top of `invoice/sales/models.py` (if not already present), then add at the end of the file:

```python
class NotificationState(models.Model):
    user = models.ForeignKey(settings.AUTH_USER_MODEL, on_delete=models.CASCADE)
    gov_invoice = models.ForeignKey('gov.GovInvoice', on_delete=models.CASCADE)
    status_snapshot = models.CharField(max_length=50)
    is_read = models.BooleanField(default=False)
    is_dismissed = models.BooleanField(default=False)
    dismissed_at = models.DateTimeField(null=True, blank=True)

    class Meta:
        unique_together = ('user', 'gov_invoice')

    def __str__(self):
        return f"NotificationState({self.user}, {self.gov_invoice_id}, read={self.is_read}, dismissed={self.is_dismissed})"
```

- [ ] **Step 2: Generate migration**

Run: `cd invoice && python manage.py makemigrations sales --name notificationstate`
Expected: `Migrations for 'sales': sales/migrations/XXXX_notificationstate.py`

- [ ] **Step 3: Apply migration**

Run: `cd invoice && python manage.py migrate sales`
Expected: `Applying sales.XXXX_notificationstate... OK`

- [ ] **Step 4: Commit**

```bash
git add invoice/sales/models.py invoice/sales/migrations/*notificationstate*
git commit -m "feat: add NotificationState model for read/dismiss tracking"
```

---

### Task 2: Add mark_all_read and dismiss_notification views

**Files:**
- Modify: `invoice/sales/views.py` (add two new views after `ngsign_pending_api`)

- [ ] **Step 1: Add the views**

Add after the `ngsign_pending_api` function (after line ~2587) in `invoice/sales/views.py`:

```python
@login_required
@require_POST
def mark_all_notifications_read(request):
    """Mark all active (non-terminal, non-dismissed) notifications as read for the current user."""
    from gov.models import GovInvoice
    from .models import NotificationState

    TERMINAL = {'TTN_SIGNED', 'TTN_TRANSFERED', 'CANCELLED'}

    active_govs = (
        GovInvoice.objects
        .exclude(ngsign_status__in=TERMINAL)
        .exclude(ngsign_status__isnull=True)
        .exclude(ngsign_status='')
    )

    for gi in active_govs:
        state, _ = NotificationState.objects.get_or_create(
            user=request.user,
            gov_invoice=gi,
            defaults={'status_snapshot': gi.ngsign_status, 'is_read': True},
        )
        if not _:
            # Only update if not currently dismissed with matching status
            if state.is_dismissed and state.status_snapshot == gi.ngsign_status:
                continue
            state.is_read = True
            state.status_snapshot = gi.ngsign_status
            state.save(update_fields=['is_read', 'status_snapshot'])

    return JsonResponse({'ok': True, 'unread_count': 0})


@login_required
@require_POST
def dismiss_notification(request, gov_invoice_id):
    """Dismiss a single notification by GovInvoice ID."""
    from gov.models import GovInvoice
    from .models import NotificationState

    gi = get_object_or_404(GovInvoice, id=gov_invoice_id)

    NotificationState.objects.update_or_create(
        user=request.user,
        gov_invoice=gi,
        defaults={
            'is_dismissed': True,
            'status_snapshot': gi.ngsign_status,
            'dismissed_at': timezone.now(),
        },
    )

    return JsonResponse({'ok': True})
```

- [ ] **Step 2: Commit**

```bash
git add invoice/sales/views.py
git commit -m "feat: add mark_all_read and dismiss_notification views"
```

---

### Task 3: Add notifications_page view

**Files:**
- Modify: `invoice/sales/views.py` (add after dismiss_notification)

- [ ] **Step 1: Add the view**

Add after `dismiss_notification` in `invoice/sales/views.py`:

```python
@login_required
def notifications_page(request):
    """Full notifications history page with filtering and pagination."""
    from gov.models import GovInvoice
    from .models import NotificationState

    STATUS_TABS = {
        'to_sign': {'CREATED', 'CONFIGURED'},
        'errors': {'ERROR', 'TTN_REJECTED', 'TTN_NOTTRANSFERED'},
        'in_progress': {'SUBMITTING', 'SIGNED', 'MIXED'},
        'done': {'TTN_SIGNED', 'TTN_TRANSFERED', 'CANCELLED'},
    }

    active_tab = request.GET.get('tab', 'all')

    govs = (
        GovInvoice.objects
        .exclude(ngsign_status__isnull=True)
        .exclude(ngsign_status='')
        .select_related('invoice__client', 'credit_note__client')
        .order_by('-created_at')
    )

    if active_tab in STATUS_TABS:
        govs = govs.filter(ngsign_status__in=STATUS_TABS[active_tab])

    # Prefetch notification states for current user
    user_states = {
        ns.gov_invoice_id: ns
        for ns in NotificationState.objects.filter(
            user=request.user,
            gov_invoice__in=govs,
        )
    }

    items = []
    for gi in govs:
        state = user_states.get(gi.id)

        # Determine read/dismissed, handle reappearance
        is_read = False
        is_dismissed = False
        if state:
            if state.status_snapshot == gi.ngsign_status:
                is_read = state.is_read
                is_dismissed = state.is_dismissed
            else:
                # Status changed — reset stale state
                state.is_read = False
                state.is_dismissed = False
                state.dismissed_at = None
                state.status_snapshot = gi.ngsign_status
                state.save(update_fields=['is_read', 'is_dismissed', 'dismissed_at', 'status_snapshot'])

        if gi.invoice:
            doc_type = 'Facture'
            doc_number = gi.invoice.uniqueId
            client_name = gi.invoice.client.clientname if gi.invoice.client else ''
            detail_url = reverse('invoice_detail', args=[gi.invoice.id])
        elif gi.credit_note:
            doc_type = 'Avoir'
            doc_number = gi.credit_note.uniqueId
            client_name = gi.credit_note.client.clientname if gi.credit_note.client else ''
            detail_url = reverse('avoir_detail', args=[gi.credit_note.id])
        else:
            continue

        items.append({
            'id': gi.id,
            'doc_type': doc_type,
            'doc_number': doc_number,
            'client_name': client_name,
            'status': gi.ngsign_status,
            'notes': gi.notes or '',
            'date': gi.created_at,
            'detail_url': detail_url,
            'is_read': is_read,
            'is_dismissed': is_dismissed,
        })

    paginator = Paginator(items, 50)
    page_number = request.GET.get('page')
    page_obj = paginator.get_page(page_number)

    # Tab counts
    all_govs = (
        GovInvoice.objects
        .exclude(ngsign_status__isnull=True)
        .exclude(ngsign_status='')
    )
    tab_counts = {
        'all': all_govs.count(),
        'to_sign': all_govs.filter(ngsign_status__in=STATUS_TABS['to_sign']).count(),
        'errors': all_govs.filter(ngsign_status__in=STATUS_TABS['errors']).count(),
        'in_progress': all_govs.filter(ngsign_status__in=STATUS_TABS['in_progress']).count(),
        'done': all_govs.filter(ngsign_status__in=STATUS_TABS['done']).count(),
    }

    return render(request, 'sales/notifications.html', {
        'page_obj': page_obj,
        'active_tab': active_tab,
        'tab_counts': tab_counts,
    })
```

- [ ] **Step 2: Commit**

```bash
git add invoice/sales/views.py
git commit -m "feat: add notifications history page view"
```

---

### Task 4: Modify ngsign_pending_api to respect read/dismissed state

**Files:**
- Modify: `invoice/sales/views.py` — the existing `ngsign_pending_api` function (lines ~2517-2586)

- [ ] **Step 1: Replace the function**

Replace the entire `ngsign_pending_api` function with this updated version that checks `NotificationState`:

```python
@login_required
@require_GET
def ngsign_pending_api(request):
    """Return all GovInvoice records with non-terminal ngsign_status, grouped by category."""
    from django.urls import reverse
    from django.utils import timezone
    from gov.models import GovInvoice
    from gov.ngsign.client import get_pds_url
    from .models import NotificationState

    TO_SIGN = {'CREATED', 'CONFIGURED'}
    ERRORS = {'ERROR', 'TTN_REJECTED', 'TTN_NOTTRANSFERED'}
    STALE_SECONDS = 60

    gov_invoices = (
        GovInvoice.objects
        .exclude(ngsign_status__in=['TTN_SIGNED', 'TTN_TRANSFERED', 'CANCELLED'])
        .exclude(ngsign_status__isnull=True)
        .exclude(ngsign_status='')
        .select_related('invoice__client', 'credit_note__client')
    )

    # Prefetch notification states for current user
    user_states = {
        ns.gov_invoice_id: ns
        for ns in NotificationState.objects.filter(
            user=request.user,
            gov_invoice__in=gov_invoices,
        )
    }

    now = timezone.now()
    to_sign = []
    errors = []
    in_progress = []
    unread_count = 0

    for gi in gov_invoices:
        if gi.invoice:
            doc_type = 'invoice'
            doc_number = gi.invoice.uniqueId
            client_name = gi.invoice.client.clientname if gi.invoice.client else ''
            detail_url = reverse('invoice_detail', args=[gi.invoice.id])
        elif gi.credit_note:
            doc_type = 'avoir'
            doc_number = gi.credit_note.uniqueId
            client_name = gi.credit_note.client.clientname if gi.credit_note.client else ''
            detail_url = reverse('avoir_detail', args=[gi.credit_note.id])
        else:
            continue

        # Stale detection: SUBMITTING for >60s is treated as ERROR
        status = gi.ngsign_status
        if status == 'SUBMITTING' and gi.submitted_at and (now - gi.submitted_at).total_seconds() > STALE_SECONDS:
            status = 'ERROR'
            gi.ngsign_status = 'ERROR'
            gi.notes = 'Soumission expirée (délai dépassé).'
            gi.save(update_fields=['ngsign_status', 'notes'])

        # Check notification state
        state = user_states.get(gi.id)
        is_read = False
        if state:
            if state.status_snapshot == status:
                # Status unchanged — respect saved state
                if state.is_dismissed:
                    continue  # Skip dismissed items
                is_read = state.is_read
            else:
                # Status changed — reset stale state
                state.is_read = False
                state.is_dismissed = False
                state.dismissed_at = None
                state.status_snapshot = status
                state.save(update_fields=['is_read', 'is_dismissed', 'dismissed_at', 'status_snapshot'])

        if not is_read:
            unread_count += 1

        item = {
            'id': gi.id,
            'doc_type': doc_type,
            'doc_number': doc_number,
            'client_name': client_name,
            'status': status,
            'detail_url': detail_url,
            'pds_url': get_pds_url(gi.ngsign_transaction_uuid) if gi.ngsign_transaction_uuid else None,
            'notes': gi.notes or '',
            'is_read': is_read,
        }

        if status in TO_SIGN:
            to_sign.append(item)
        elif status in ERRORS:
            errors.append(item)
        else:
            in_progress.append(item)

    return JsonResponse({
        'to_sign': to_sign,
        'errors': errors,
        'in_progress': in_progress,
        'total': len(to_sign) + len(errors) + len(in_progress),
        'unread_count': unread_count,
    })
```

- [ ] **Step 2: Commit**

```bash
git add invoice/sales/views.py
git commit -m "feat: ngsign_pending_api respects read/dismissed state"
```

---

### Task 5: Add URL patterns

**Files:**
- Modify: `invoice/sales/urls.py`

- [ ] **Step 1: Add imports**

Add `mark_all_notifications_read, dismiss_notification, notifications_page` to the import block at the top of `invoice/sales/urls.py`.

- [ ] **Step 2: Add URL patterns**

Add before the closing `]` of `urlpatterns`:

```python
    # Notifications
    path('notifications/', notifications_page, name='notifications_page'),
    path('api/ngsign/notifications/read/', mark_all_notifications_read, name='notifications-mark-read'),
    path('api/ngsign/notifications/<int:gov_invoice_id>/dismiss/', dismiss_notification, name='notifications-dismiss'),
```

- [ ] **Step 3: Commit**

```bash
git add invoice/sales/urls.py
git commit -m "feat: add notification URL patterns"
```

---

### Task 6: Update bell dropdown HTML and CSS in base.html

**Files:**
- Modify: `invoice/templates/partials/base.html`

- [ ] **Step 1: Add new CSS styles**

Add after the `.notification-spinner` / `@keyframes notif-spin` block (after line ~248), before `/* Modern Sidebar */`:

```css
      .notification-item.read { opacity: 0.6; }

      .notification-item-actions {
        display: flex;
        align-items: center;
        gap: 4px;
      }

      .notification-dismiss {
        background: none;
        border: none;
        color: rgba(255, 255, 255, 0.3);
        font-size: 0.75rem;
        cursor: pointer;
        padding: 2px 4px;
        line-height: 1;
        transition: color 0.2s;
      }

      .notification-dismiss:hover { color: #e74c3c; }

      .notification-footer {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 8px 14px;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
      }

      .notification-footer a,
      .notification-footer button {
        font-size: 0.7rem;
        color: #3498db;
        text-decoration: none;
        background: none;
        border: none;
        cursor: pointer;
        padding: 0;
      }

      .notification-footer a:hover,
      .notification-footer button:hover { text-decoration: underline; }
```

- [ ] **Step 2: Update dropdown HTML**

Replace the notification dropdown HTML (lines ~673-681) with:

```html
        <div class="notification-dropdown" id="notificationDropdown">
          <div class="notification-header">
            <span class="notification-header-title">Documents NGSign</span>
            <div style="display:flex;align-items:center;gap:8px;">
              <div id="notificationBadges"></div>
              <button type="button" id="markAllReadBtn" style="display:none;font-size:0.65rem;color:#3498db;background:none;border:none;cursor:pointer;" onclick="markAllRead()">Tout lire</button>
            </div>
          </div>
          <div id="notificationContent">
            <div class="notification-empty">Chargement...</div>
          </div>
          <div class="notification-footer">
            <a href="/notifications/">Voir tout</a>
          </div>
        </div>
```

- [ ] **Step 3: Add error detail modal**

Add right before the closing `{% endif %}` that ends the script-level `{% if user.is_authenticated %}` block (around line ~1060, after the `</script>` tag):

```html
<!-- Error Detail Modal -->
<div class="modal fade" id="notifErrorModal" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content" style="background:#1a1d29;border:1px solid rgba(255,255,255,0.1);color:white;">
      <div class="modal-header border-bottom" style="border-color:rgba(255,255,255,0.1) !important;">
        <h6 class="modal-title" id="notifErrorModalTitle">Détails de l'erreur</h6>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
      </div>
      <div class="modal-body">
        <p id="notifErrorModalBody" style="white-space:pre-wrap;font-size:0.85rem;color:rgba(255,255,255,0.8);"></p>
      </div>
      <div class="modal-footer border-top" style="border-color:rgba(255,255,255,0.1) !important;">
        <button type="button" class="btn btn-sm btn-outline-light" data-bs-dismiss="modal">Fermer</button>
      </div>
    </div>
  </div>
</div>
```

- [ ] **Step 4: Commit**

```bash
git add invoice/templates/partials/base.html
git commit -m "feat: update bell dropdown HTML/CSS, add error modal"
```

---

### Task 7: Update notification JavaScript in base.html

**Files:**
- Modify: `invoice/templates/partials/base.html` — the inline `<script>` block (lines ~883-1058)

- [ ] **Step 1: Replace the notification script**

Replace the entire `<script>(function() { ... })();</script>` block with:

```javascript
<script>
(function() {
  const bell = document.getElementById('notificationBell');
  const dropdown = document.getElementById('notificationDropdown');
  const badge = document.getElementById('notificationBadge');
  const content = document.getElementById('notificationContent');
  const badges = document.getElementById('notificationBadges');
  const markAllBtn = document.getElementById('markAllReadBtn');
  if (!bell) return;

  let pollInterval = null;

  // CSRF helper
  function getCSRFToken() {
    const c = document.cookie.split(';').map(c => c.trim()).find(c => c.startsWith('csrftoken='));
    return c ? c.split('=')[1] : '';
  }

  // Toast helper
  window.showToast = function(message, type) {
    type = type || 'info';
    let container = document.querySelector('.toast-container');
    if (!container) {
      const wrapper = document.createElement('div');
      wrapper.setAttribute('aria-live', 'polite');
      wrapper.setAttribute('aria-atomic', 'true');
      container = document.createElement('div');
      container.className = 'toast-container position-fixed top-0 end-0 p-3';
      wrapper.appendChild(container);
      document.body.appendChild(wrapper);
    }
    const iconMap = {
      success: 'bi-check-circle-fill',
      error: 'bi-x-circle-fill',
      warning: 'bi-exclamation-triangle-fill',
      info: 'bi-info-circle-fill',
    };
    const toastEl = document.createElement('div');
    toastEl.className = 'toast modern-toast align-items-center border-0 mb-2 text-white toast-' + type;
    toastEl.setAttribute('role', 'alert');
    toastEl.setAttribute('data-bs-delay', '4000');
    toastEl.innerHTML =
      '<div class="d-flex">' +
        '<div class="toast-body"><i class="bi ' + (iconMap[type] || iconMap.info) + ' me-2"></i>' + message + '</div>' +
        '<button type="button" class="btn-close ' + (type !== 'warning' ? 'btn-close-white' : '') + ' me-2 m-auto" data-bs-dismiss="toast"></button>' +
      '</div>';
    container.appendChild(toastEl);
    new bootstrap.Toast(toastEl).show();
    toastEl.addEventListener('hidden.bs.toast', function() { toastEl.remove(); });
  };

  // Toggle dropdown
  bell.addEventListener('click', function(e) {
    e.stopPropagation();
    dropdown.classList.toggle('show');
  });

  // Close on outside click
  document.addEventListener('click', function(e) {
    if (!dropdown.contains(e.target) && !bell.contains(e.target)) {
      dropdown.classList.remove('show');
    }
  });

  function renderNotifications(data) {
    // Update badge with unread count
    if (data.unread_count > 0) {
      badge.textContent = data.unread_count;
      badge.style.display = 'flex';
    } else {
      badge.style.display = 'none';
    }

    // Show/hide mark all read button
    markAllBtn.style.display = data.unread_count > 0 ? 'inline' : 'none';

    // Header badges
    let badgeHtml = '';
    if (data.to_sign.length) badgeHtml += '<span style="background:#f39c12;color:#000;border-radius:10px;padding:2px 7px;font-size:0.65rem;margin-left:4px;">' + data.to_sign.length + '</span>';
    if (data.errors.length) badgeHtml += '<span style="background:#e74c3c;color:#fff;border-radius:10px;padding:2px 7px;font-size:0.65rem;margin-left:4px;">' + data.errors.length + '</span>';
    if (data.in_progress.length) badgeHtml += '<span style="background:#2ecc71;color:#000;border-radius:10px;padding:2px 7px;font-size:0.65rem;margin-left:4px;">' + data.in_progress.length + '</span>';
    badges.innerHTML = badgeHtml;

    if (data.total === 0) {
      content.innerHTML = '<div class="notification-empty">Aucun document en cours.</div>';
      stopPolling();
      return;
    }

    let html = '';

    if (data.to_sign.length) {
      html += '<div class="notification-group-label to-sign">A signer (' + data.to_sign.length + ')</div>';
      data.to_sign.forEach(function(item) {
        html += renderItem(item, 'sign');
      });
    }

    if (data.errors.length) {
      html += '<div class="notification-group-label errors">Erreurs (' + data.errors.length + ')</div>';
      data.errors.forEach(function(item) {
        html += renderItem(item, 'error');
      });
    }

    if (data.in_progress.length) {
      html += '<div class="notification-group-label in-progress">En cours (' + data.in_progress.length + ')</div>';
      data.in_progress.forEach(function(item) {
        if (item.status === 'SUBMITTING') {
          html += renderItem(item, 'spinner');
        } else {
          html += renderItem(item, 'check');
        }
      });
    }

    content.innerHTML = html;
    startPolling();
  }

  function escapeHtml(str) {
    var div = document.createElement('div');
    div.textContent = str;
    return div.innerHTML;
  }

  function renderItem(item, actionType) {
    let actionHtml = '';
    if (actionType === 'sign' && item.pds_url) {
      actionHtml = '<button class="notification-item-action sign" onclick="window.open(\'' + item.pds_url + '\', \'_blank\')">Signer</button>';
    } else if (actionType === 'error') {
      actionHtml = '<button class="notification-item-action view" onclick="showErrorModal(' + JSON.stringify(item.doc_type) + ', ' + JSON.stringify(item.doc_number) + ', ' + JSON.stringify(item.client_name) + ', ' + JSON.stringify(item.notes) + ')">Détails</button>';
    } else if (actionType === 'check') {
      actionHtml = '<button class="notification-item-action check" onclick="ngsignCheck(\'' + item.detail_url + '\')">Check</button>';
    } else if (actionType === 'spinner') {
      actionHtml = '<span class="notification-spinner"></span>';
    }

    var readClass = item.is_read ? ' read' : '';

    return '<div class="notification-item' + readClass + '" data-gov-id="' + item.id + '">' +
      '<div class="notification-item-info">' +
        '<span class="notification-item-number">' + escapeHtml(item.doc_number) + '</span>' +
        '<span class="notification-item-client">' + escapeHtml(item.client_name) + '</span>' +
      '</div>' +
      '<div class="notification-item-actions">' +
        actionHtml +
        '<button class="notification-dismiss" onclick="dismissNotification(' + item.id + ', this)" title="Masquer">&times;</button>' +
      '</div>' +
    '</div>';
  }

  // Dismiss a single notification
  window.dismissNotification = function(govInvoiceId, btnEl) {
    var itemEl = btnEl.closest('.notification-item');
    if (itemEl) itemEl.style.opacity = '0.3';

    fetch('/api/ngsign/notifications/' + govInvoiceId + '/dismiss/', {
      method: 'POST',
      headers: { 'X-CSRFToken': getCSRFToken() },
    })
    .then(function(r) { return r.json(); })
    .then(function() { fetchNotifications(); })
    .catch(function() {
      if (itemEl) itemEl.style.opacity = '';
    });
  };

  // Mark all as read
  window.markAllRead = function() {
    fetch('/api/ngsign/notifications/read/', {
      method: 'POST',
      headers: { 'X-CSRFToken': getCSRFToken() },
    })
    .then(function(r) { return r.json(); })
    .then(function(data) {
      badge.style.display = 'none';
      markAllBtn.style.display = 'none';
      fetchNotifications();
    })
    .catch(function() {});
  };

  // Show error detail modal
  window.showErrorModal = function(docType, docNumber, clientName, notes) {
    document.getElementById('notifErrorModalTitle').textContent = docType + ' ' + docNumber + ' \u2014 ' + clientName;
    document.getElementById('notifErrorModalBody').textContent = notes || 'Aucun détail disponible.';
    new bootstrap.Modal(document.getElementById('notifErrorModal')).show();
  };

  window.fetchNotifications = function() {
    fetch('/api/ngsign/pending/')
      .then(function(r) { return r.json(); })
      .then(function(data) { renderNotifications(data); })
      .catch(function() {});
  };

  function startPolling() {
    if (pollInterval) return;
    pollInterval = setInterval(function() {
      if (document.hidden) return;
      fetchNotifications();
    }, 30000);
  }

  function stopPolling() {
    if (pollInterval) {
      clearInterval(pollInterval);
      pollInterval = null;
    }
  }

  // Pause/resume on tab visibility
  document.addEventListener('visibilitychange', function() {
    if (!document.hidden && badge.style.display !== 'none') {
      fetchNotifications();
    }
  });

  // Check button action from dropdown
  window.ngsignCheck = function(detailUrl) {
    window.location.href = detailUrl;
  };

  // Initial fetch
  document.addEventListener('DOMContentLoaded', function() {
    fetchNotifications();
  });
})();
</script>
```

- [ ] **Step 2: Commit**

```bash
git add invoice/templates/partials/base.html
git commit -m "feat: update notification JS — dismiss, mark read, error modal"
```

---

### Task 8: Create notifications page template

**Files:**
- Create: `invoice/templates/sales/notifications.html`

- [ ] **Step 1: Create the template**

Create `invoice/templates/sales/notifications.html`:

```html
{% extends 'partials/base.html' %}

{% block title %}Notifications{% endblock %}

{% block css %}
<style>
  .status-badge {
    display: inline-flex;
    align-items: center;
    gap: 4px;
    padding: 3px 10px;
    border-radius: 20px;
    font-size: 0.7rem;
    font-weight: 600;
  }
  .status-badge.to-sign { background: rgba(243,156,18,0.15); color: #f39c12; }
  .status-badge.error { background: rgba(231,76,60,0.15); color: #e74c3c; }
  .status-badge.in-progress { background: rgba(46,204,113,0.15); color: #2ecc71; }
  .status-badge.done { background: rgba(149,165,166,0.15); color: #95a5a6; }

  .notif-row.is-read { opacity: 0.6; }
  .notif-row.is-dismissed { opacity: 0.4; text-decoration: line-through; }

  .tab-pills .nav-link {
    color: rgba(255,255,255,0.6);
    border-radius: 20px;
    padding: 4px 14px;
    font-size: 0.8rem;
    font-weight: 500;
  }
  .tab-pills .nav-link.active {
    background: var(--accent-blue);
    color: white;
  }
  .tab-pills .nav-link:hover:not(.active) {
    color: white;
  }
  .tab-count {
    font-size: 0.65rem;
    background: rgba(255,255,255,0.15);
    border-radius: 10px;
    padding: 1px 6px;
    margin-left: 4px;
  }
</style>
{% endblock %}

{% block main %}
<div class="page-header d-flex justify-content-between align-items-center">
  <div>
    <h1>Notifications</h1>
    <p>Historique des documents NGSign</p>
  </div>
  <div class="page-header-actions">
    <button type="button" class="btn btn-modern-primary btn-modern btn-sm" onclick="markAllReadPage()">
      <i class="bi bi-check2-all me-1"></i>Tout marquer comme lu
    </button>
  </div>
</div>

<!-- Filter Tabs -->
<div class="modern-card mb-3">
  <ul class="nav tab-pills gap-2 flex-wrap">
    <li class="nav-item">
      <a class="nav-link {% if active_tab == 'all' %}active{% endif %}" href="?tab=all">
        Tous <span class="tab-count">{{ tab_counts.all }}</span>
      </a>
    </li>
    <li class="nav-item">
      <a class="nav-link {% if active_tab == 'to_sign' %}active{% endif %}" href="?tab=to_sign">
        A signer <span class="tab-count">{{ tab_counts.to_sign }}</span>
      </a>
    </li>
    <li class="nav-item">
      <a class="nav-link {% if active_tab == 'errors' %}active{% endif %}" href="?tab=errors">
        Erreurs <span class="tab-count">{{ tab_counts.errors }}</span>
      </a>
    </li>
    <li class="nav-item">
      <a class="nav-link {% if active_tab == 'in_progress' %}active{% endif %}" href="?tab=in_progress">
        En cours <span class="tab-count">{{ tab_counts.in_progress }}</span>
      </a>
    </li>
    <li class="nav-item">
      <a class="nav-link {% if active_tab == 'done' %}active{% endif %}" href="?tab=done">
        Terminés <span class="tab-count">{{ tab_counts.done }}</span>
      </a>
    </li>
  </ul>
</div>

{% if page_obj %}
<div class="modern-card">
  <div class="table-responsive">
    <table class="table modern-table mb-0">
      <thead>
        <tr>
          <th>Statut</th>
          <th>Type</th>
          <th>N° Document</th>
          <th>Client</th>
          <th>Date</th>
          <th style="max-width:200px;">Notes</th>
          <th class="text-end">Actions</th>
        </tr>
      </thead>
      <tbody>
        {% for item in page_obj %}
        <tr class="notif-row{% if item.is_read %} is-read{% endif %}{% if item.is_dismissed %} is-dismissed{% endif %}" id="notif-row-{{ item.id }}">
          <td>
            {% if item.status == 'CREATED' or item.status == 'CONFIGURED' %}
              <span class="status-badge to-sign"><i class="bi bi-circle-fill" style="font-size:0.35rem;"></i> {{ item.status }}</span>
            {% elif item.status == 'ERROR' or item.status == 'TTN_REJECTED' or item.status == 'TTN_NOTTRANSFERED' %}
              <span class="status-badge error"><i class="bi bi-circle-fill" style="font-size:0.35rem;"></i> {{ item.status }}</span>
            {% elif item.status == 'TTN_SIGNED' or item.status == 'TTN_TRANSFERED' or item.status == 'CANCELLED' %}
              <span class="status-badge done"><i class="bi bi-circle-fill" style="font-size:0.35rem;"></i> {{ item.status }}</span>
            {% else %}
              <span class="status-badge in-progress"><i class="bi bi-circle-fill" style="font-size:0.35rem;"></i> {{ item.status }}</span>
            {% endif %}
          </td>
          <td>{{ item.doc_type }}</td>
          <td class="fw-bold">{{ item.doc_number }}</td>
          <td>{{ item.client_name }}</td>
          <td>{{ item.date|date:"d/m/Y H:i" }}</td>
          <td style="max-width:200px;">
            {% if item.notes %}
              <span class="text-truncate d-inline-block" style="max-width:190px;" title="{{ item.notes }}">{{ item.notes }}</span>
            {% else %}
              <span class="text-muted small">—</span>
            {% endif %}
          </td>
          <td class="text-end">
            <div class="btn-group btn-group-sm">
              <a href="{{ item.detail_url }}" class="btn btn-outline-primary" title="Voir document">
                <i class="bi bi-eye"></i>
              </a>
              {% if item.status == 'ERROR' or item.status == 'TTN_REJECTED' or item.status == 'TTN_NOTTRANSFERED' %}
              <button type="button" class="btn btn-outline-warning" title="Détails erreur"
                      onclick="showErrorModal('{{ item.doc_type|escapejs }}', '{{ item.doc_number|escapejs }}', '{{ item.client_name|escapejs }}', '{{ item.notes|escapejs }}')">
                <i class="bi bi-exclamation-triangle"></i>
              </button>
              {% endif %}
              {% if not item.is_dismissed %}
              <button type="button" class="btn btn-outline-secondary" title="Masquer"
                      onclick="dismissFromPage({{ item.id }})">
                <i class="bi bi-x-lg"></i>
              </button>
              {% endif %}
            </div>
          </td>
        </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>

  {% if page_obj.has_other_pages %}
  <div class="d-flex justify-content-center py-3">
    <nav>
      <ul class="pagination pagination-sm mb-0">
        {% if page_obj.has_previous %}
        <li class="page-item">
          <a class="page-link" href="?tab={{ active_tab }}&page={{ page_obj.previous_page_number }}">
            <i class="bi bi-chevron-left"></i>
          </a>
        </li>
        {% endif %}
        {% for num in page_obj.paginator.page_range %}
          {% if page_obj.number == num %}
          <li class="page-item active"><span class="page-link">{{ num }}</span></li>
          {% elif num > page_obj.number|add:"-3" and num < page_obj.number|add:"3" %}
          <li class="page-item">
            <a class="page-link" href="?tab={{ active_tab }}&page={{ num }}">{{ num }}</a>
          </li>
          {% endif %}
        {% endfor %}
        {% if page_obj.has_next %}
        <li class="page-item">
          <a class="page-link" href="?tab={{ active_tab }}&page={{ page_obj.next_page_number }}">
            <i class="bi bi-chevron-right"></i>
          </a>
        </li>
        {% endif %}
      </ul>
    </nav>
  </div>
  {% endif %}
</div>
{% else %}
<div class="modern-card">
  <div class="text-center py-5 text-muted">
    <i class="bi bi-bell-slash fs-1 mb-3 d-block"></i>
    <p>Aucune notification.</p>
  </div>
</div>
{% endif %}
{% endblock %}

{% block scripts %}
<script>
function getCSRFToken() {
  var c = document.cookie.split(';').map(function(c) { return c.trim(); }).find(function(c) { return c.startsWith('csrftoken='); });
  return c ? c.split('=')[1] : '';
}

function markAllReadPage() {
  fetch('/api/ngsign/notifications/read/', {
    method: 'POST',
    headers: { 'X-CSRFToken': getCSRFToken() },
  })
  .then(function(r) { return r.json(); })
  .then(function() { location.reload(); })
  .catch(function() { showToast('Erreur lors du marquage', 'error'); });
}

function dismissFromPage(govInvoiceId) {
  var row = document.getElementById('notif-row-' + govInvoiceId);
  if (row) row.style.opacity = '0.3';

  fetch('/api/ngsign/notifications/' + govInvoiceId + '/dismiss/', {
    method: 'POST',
    headers: { 'X-CSRFToken': getCSRFToken() },
  })
  .then(function(r) { return r.json(); })
  .then(function() { location.reload(); })
  .catch(function() {
    if (row) row.style.opacity = '';
    showToast('Erreur lors du masquage', 'error');
  });
}
</script>
{% endblock %}
```

- [ ] **Step 2: Commit**

```bash
git add invoice/templates/sales/notifications.html
git commit -m "feat: add notifications history page template"
```

---

### Task 9: Manual testing checklist

- [ ] **Step 1: Run the dev server**

Run: `cd invoice && python manage.py runserver`

- [ ] **Step 2: Test bell dropdown**

1. Open the app, verify the bell icon loads with the correct badge count (`unread_count`)
2. Click bell — verify dropdown shows notifications grouped by status
3. Verify "Tout lire" button appears when there are unread items
4. Click "Tout lire" — verify badge disappears and items get muted styling
5. Verify items get muted (`opacity: 0.6`) styling when read

- [ ] **Step 3: Test dismiss**

1. Click × on a notification item — verify it fades out and disappears
2. Verify badge count decreases
3. If the dismissed item's GovInvoice status changes later, verify it reappears

- [ ] **Step 4: Test error modal**

1. Find an error notification in the dropdown
2. Click "Détails" — verify Bootstrap modal opens with error text
3. Verify modal title shows document number and client name
4. Click "Fermer" — verify modal closes

- [ ] **Step 5: Test notifications page**

1. Click "Voir tout" in dropdown — verify it navigates to `/notifications/`
2. Verify filter tabs work (Tous, A signer, Erreurs, En cours, Terminés)
3. Verify tab counts are correct
4. Verify "Tout marquer comme lu" button works
5. Verify dismiss button works on individual rows
6. Verify error detail button works on error rows
7. Verify pagination works if >50 items

- [ ] **Step 6: Final commit (if any fixes needed)**

```bash
git add -A
git commit -m "fix: notification system adjustments from manual testing"
```
